# Hermes

Hermes is available as an early-access ACP provider in this fork.

Enable a Hermes provider instance in Settings on the machine where the `hermes` CLI and, by
default, the Hermes gateway are running. T3 Code discovers Hermes models, slash commands, and
skills from that local installation. Hermes authentication remains owned by the CLI.

# Sync Ledger

Working memory for the sync-upstream skill. Record only what changes how a sync is performed: standing decisions, watchpoint triggers, and the full-audit marker. Never record what the code or git history already shows, and never grow this into a fork feature list.

Self-cleaning rules (apply during every sync's ledger update):

- Remove a watchpoint after two consecutive syncs in which its path was untouched by upstream or merged trivially.
- Remove a standing decision when the code it governs no longer exists on `dev`, or upstream ships the change named in its revisit condition (then re-decide with the user).
- Update the audit marker after every full audit or explicit postponement.

## Standing decisions

- **Claude skill picker — keep both layers** (2026-07-24). Fork's `listSkillsForCwd`/`adapter.listSkills` (SDK `reload_skills`, per-thread cwd) is the primary source; upstream's `discoverClaudeSkills` provider-status scan is the fallback. Upstream's scan receives the server's own `ServerConfig.cwd`, so dropping the fork path would lose project-scoped skills. Revisit only if upstream makes discovery per-thread-cwd.
- **Provider command flow — keep steering and title-pin guards** (2026-07-24). Fork's failed-steer handling preserves the running session and active turn, while ordinary startup failures still use upstream's error transition. `titlePinned` protects explicit CLI titles before and after asynchronous title generation. Both remain compatible with upstream interrupt/title generation. Revisit if upstream distinguishes failed steers itself or persists an equivalent explicit-title pin.
- **Repository identity — deliberate hybrid, do not deduplicate** (2026-07-24, reaffirmed 2026-08-11). Fork persists identity (migration 035 + write-time enrichment in `OrchestrationEngine.ts`); upstream resolves at query time, and the merged `ProjectionSnapshotQuery.ts` writes resolved values back into the fork's column as a cache with stored-value fallback. Both paths cover cases the other cannot: removing persistence reintroduces archive/project-group fragmentation when a checkout disappears. Persisted identity is read-model fallback metadata, not sufficient authority for pull-request mutations; actionable operations must verify a live checkout identity or use a healthy sibling checkout.
- **Event replay bounds — complementary, not duplication** (2026-07-24, updated 2026-07-29). Upstream's `SHELL_RESUME_MAX_GAP` bounds shell resume; fork's aggregate-filtered `readEvents` (`ws.ts` thread-detail catch-up) filters per-thread replay at the SQL level. Keep both. Upstream removed the unused public `orchestration.replayEvents` RPC on 2026-07-29; do not restore that dead RPC or its repository-identity enrichment tests.
- **Effect beta bumps expose fork-only APIs first** (2026-07-28, updated 2026-08-05). Typecheck the server immediately after an Effect beta bump and read cascades bottom-up. Beta 102→103 removed `Schema.UnknownFromJsonString`; fork provider/session-import code must use `Schema.fromJsonString(Schema.Unknown)`. Fix concrete removed APIs before diagnosing the resulting `any`/`unknown` requirement-channel cascade.
- **Beta flags resolve through a hook, never the raw setting** (2026-07-28, updated 2026-08-08). Any fork code branching on a beta flag must call its hook (`useLegacySidebarEnabled()`, `useThreadListV2Enabled()`), never read the raw setting: a raw read treats "never chosen" as "off" and ignores the build-stage default. Key names changed on 2026-08-08 — see [Beta flags are fresh keys with inverted meaning].
- **Project settings live on upstream's contextual project page** (2026-07-28, updated 2026-08-10). Project settings are no longer a Settings route: `/settings/projects` and `/settings/projects/$projectKey` are gone, replaced by `/projects/$projectKey` (`ProjectSettingsPage`), reached from the sidebar gear and the command palette's contextual action. `ProjectSettingsPanel.tsx` keeps upstream's per-checkout selector and manual icon picker alongside four ported fork capabilities: the accent picker (in upstream's **Project** section, after Project icon), archived-thread navigation filtered by the _logical_ group key (its own **Threads** section), CLI session import (in the **Checkout** section, targeting `selectedCheckout` — the old per-member card buttons are gone with the cards), and sidebar scope/hidden-filter cleanup before upstream's post-delete `/` redirect. `inheritedEnvModeLabels` applies only to the single-checkout branch; upstream owns the grouped "per checkout" copy. Project favicon rendering must preserve the fork accent as its fallback tint across sidebar, archive, search, command, chat-header, and mobile surfaces. Do not restore the dead Sidebar dialog or the deleted settings routes. Revisit if upstream re-adds a projects listing.
- **Project settings are not in the settings-search catalog** (2026-08-10). The user chose to port every fork capability onto upstream's contextual page rather than trim (Option A), which cost the eight `/settings/projects` search entries: `SettingsPath` is a static union and cannot express `/projects/$projectKey`. `settingsSearch.test.ts` pins this with a "no longer indexes project settings" case. If upstream ever parameterizes settings search, re-add `project-thread-accent`, `project-import-session` and `project-archived-threads` alongside the five upstream ones.
- **Sidebar indicators and thread actions coexist** (2026-07-30, updated 2026-08-08 — now in `Sidebar.tsx`; the fork actions moved into the shared menu builder, see [Thread action menu is one shared builder]). Keep upstream's terminal status, title-regeneration state, and Copy path/Copy branch alongside the fork's provider icon/compact metadata and Fork/Archive actions. When their adjacent edits conflict, retain the fork's row layout and insert upstream status into its shared metadata/accessibility slots so slim, compact, and expanded rows all remain covered. Revisit if upstream ships equivalent fork/archive actions or replaces the row/action model.
- **Environment identification keeps two independent gates** (2026-07-29, updated 2026-08-11). Upstream now owns theme-aware artwork colors and built-in-theme eligibility; custom themes deliberately fall back to the pill. `resolveSidebarStageBackdropVariant` still takes an options object: `enabled` implements the artwork/pill/none setting, while `isDevelopmentBuild` preserves the fork rule that Dev artwork never appears in packaged builds. Nightly artwork remains eligible in packaged builds. Do not collapse these back into one positional boolean or restore the removed custom-theme artwork toggle.
- **Mobile v2 rows keep fork filters around upstream lifecycle sections** (2026-07-29, updated 2026-08-05). Upstream's shared `buildThreadListV2ListItems` owns active → pending → settled ordering, pending-row metadata, the snoozed shelf, and section-divider suppression. Before that splice, both `HomeScreen.tsx` and `ThreadNavigationSidebar.tsx` must retain the fork's environment, model, project, attention, and search predicates while also passing upstream's `matchedThreadKeys` and rendering message-match excerpts. Rows must keep `resolveThreadProviderDriver` and project accents while adopting upstream lifecycle actions and `showTrailingDivider`; preserve both the accent overlay and divider gate when their row wrappers conflict. Mobile grouping mode now resolves through `state/project-grouping.ts`, while the fork's visibility filters remain a separate `HomeListOptionsProvider` concern.
- **Fork requirements override upstream AGENTS defaults** (2026-07-29). Keep upstream's broad project guidance in `AGENTS.md`, but retain the leading private-fork requirements for full local gates, `dev`/mirror-only `main` topology, focused fork testing, private build helpers, and controlled-browser verification of shared URLs. These explicitly override conflicting upstream defaults such as CI-only repo-wide checks or rebasing feature work onto mirror `main`.
- **test-t3-mobile skill carries fork hardening** (2026-07-29). `.agents/skills/test-t3-mobile/SKILL.md` deliberately diverges from upstream #4165: resolved (not hardcoded) bundle identifier and workspace names, native-compatibility precheck, per-worktree derived-data pinning, physical-device section, opt-in trigger clause in the description, and expanded troubleshooting; `references/local-setup.md` is fork-only and must never go upstream. On sync conflicts, take upstream's structural changes but preserve these additions; never revert to upstream's hardcoded `com.t3tools.t3code.dev` / `T3CodeDev.xcworkspace` constants. Revisit if upstream ships its own identity-resolution step.
- **Migration numbering** (2026-07-24, updated 2026-08-09). Fork history occupies 033–043; upstream pinning is `044_ProjectionThreadsPinned`, upstream's turns keyset index (their 037) landed as `045_ProjectionTurnsKeysetIndex`, upstream's pin-order key (their 038) landed as `046_ProjectionThreadsPinOrderKey`, upstream's project default-env-mode migration (their 039) landed as `047_ProjectionProjectsDefaultThreadEnvMode`, and upstream's project-favicon-path migration (their 040) landed as `048_ProjectionProjectFaviconPath`. Never renumber shipped fork migrations. Renumber each new upstream migration after the highest `dev` ID (currently 048) and verify ordering whenever upstream adds one. The merge stages upstream's original file too, so `git rm` the upstream-numbered copy after writing the renumbered one. Pure-upstream databases with overlapping numeric IDs are not interchangeable with fork databases because the migrator tracks only the latest numeric ID.
- **RPC authorization stays centralized and exhaustive** (2026-07-29). Use upstream's typed `apps/server/src/auth/RpcAuthorization.ts` map and never restore the old local map in `ws.ts`. Fork RPC scopes remain `serverListProviderSkills` → orchestration read, `sessionImportListCandidates` → orchestration read, and `sessionImportImport` → orchestration operate. Revisit if those methods are removed or replaced.
- **Fork README is whole-file fork-owned; resolve with `ours`** (2026-07-30, corrected 2026-08-12). `main:README.md` and `dev:README.md` share zero non-empty lines (verified 2026-08-12), so any conflict there resolves mechanically with `git checkout --ours -- README.md`. Still skim upstream's README diff for content worth porting by hand; port it as a deliberate edit, never a merge. Do not add a `.gitattributes` merge driver. Other fork files also intentionally differ from `main` and merge under their own decisions.
- **Saved-environment action row hosts both update and edit actions** (2026-07-30). In `ConnectionsSettings.tsx`'s `SavedBackendListRow`, upstream moved `ServerUpdateAction` out of the version-drift paragraph into the trailing action row — the same row the fork extends with its bearer-only "Edit" button. Keep both, upstream's update action first. Revisit if upstream adds its own edit affordance there.
- **Hermes follows shared provider background policy** (2026-07-29). `HermesDriverEnv` includes `BackgroundPolicy`, and Hermes leaves `makeManagedServerProvider.refreshInterval` unset so the same server setting, demand gate, and battery policy used by built-in providers controls its health refresh. Revisit if upstream changes the managed-provider scheduling contract again.
- **Thread outbox manager remains shared across web and mobile** (2026-07-30). Upstream's optimistic mobile enqueue was ported from `apps/mobile/src/state/thread-outbox-manager.ts` into the fork's `packages/client-runtime/src/state/threadOutboxManager.ts`. Both client drains must await `confirmQueued` before delivery; mobile keeps its queued-message intent/filter behavior, and web must not deliver a row whose durable write rolls back. Revisit if upstream moves the manager into shared client-runtime or changes enqueue durability again.
- **Indefinite snooze extends the shared timed presets** (2026-07-31, updated 2026-08-06). `Sidebar.snooze.ts` wraps the shared timed-preset helper to add capability-gated `until-woken` with a null wake time and timestamp-format-aware labels; compact wake labels use the shared client-runtime helper directly. Revisit if the shared preset helper supports indefinite snooze without weakening its mobile string-wake-time contract.
- **Settings search includes fork-only surfaces** (2026-08-01). Keep `/settings/extras` in `SettingsPath`, navigation labels/icons, and the searchable catalog with anchors for every Extras section. The archive search target belongs on the fork's filtered logical-project groups rather than restoring upstream's physical-project rendering. Revisit if upstream provides an extension registry for settings routes and search entries.
- **Pin and move-to-top coexist** (2026-08-05, updated 2026-08-08). Pinned threads sort by upstream's persistent fractional-index `pinOrderKey` (drag to reorder), with creation order only as the keyless fallback; fork `movedToTopAt` only reorders **unpinned active** threads. The two partitions never overlap, so both stay. Hide Move to top on pinned rows. Pin promotion must unsnooze when either `snoozedUntil` or `snoozedAt` is set so indefinite snoozes wake too — `thread.pin.reorder` deliberately does not unsnooze (it requires an already-pinned thread). Pinned sorting must NOT be capability-gated; only the drag affordance checks `threadPinReorder`, or mixed-version fleets would render different orders from the same data. Preserve both command/event/capability/projection/reducer contracts. Revisit if upstream adds transient active ordering or changes pin lifecycle semantics.
- **Upstream owns trial migration rollback** (2026-08-05). Protocol 2 backs up and restores the database, so trial startup runs migrations unconditionally. Do not restore persistence `setup(trial)`, trial options, migration-manifest preflight, or a persistence dependency on `resolveServiceLauncherMode`. Keep `PRAGMA busy_timeout = 5000` on the normal connection, voice middleware, native 128 MiB Bun/Node WebSocket limits, and all fork CLI commands.
- **Every new PR mutation needs `verifyLive`; project filtering stays deferred** (2026-08-13). Upstream keeps adding change-request mutations that call `requireProject(input)` plainly (this sync: `update`, `updateComment`, `setReaction`). Each one must become `requireProject(input, { verifyLive: true })` on merge, or the read model's persisted-identity fallback alone authorizes a provider write. Reads (`detail`, `activity`, `diff`, `diffFileContents`, `reviewerCandidates`) stay unguarded. Separately, in `listWorkspaceProjects` the fork deliberately defers **both** `filter.projectId` and `filter.projectIds` until after `checkoutsByRepository` is populated — upstream writes them at the top of the loop, and taking that placement would strip the sibling checkouts a mutation needs to verify identity when its own worktree is gone. Re-apply the deferral every time upstream adds a listing filter.
- **Composer content moves carry `inputOrigin`** (2026-08-13). Upstream's `moveComposerPromptAndImages` (draft repo picker) moves prompt and attachments; the fork's `inputOrigin` must travel with the text and be stripped from the drained source, matching `clearComposerContent`. Leaving it behind loses voice-transcription provenance and pins an otherwise empty source draft past `shouldRemoveDraft`. Any future upstream helper that relocates composer content needs the same treatment.
- **Auto-settle is a master gate over a per-reason toggle** (2026-08-14). `effectiveSettled` takes both the fork's required `autoSettleEnabled` (global; when false nothing auto-settles, manual settle still works) and upstream's optional `autoSettleOnMerge` (merged settles only when on; closed always settles). The master gate is checked **first**, before `changeRequestAutoSettles`. Every call site must pass both — a site omitting `autoSettleOnMerge` silently defaults it to `true` and disagrees with the rest of the app about whether a merged thread is settled (this happened to `CommandPalette.tsx` in this sync). In the web UI the merge switch is `disabled` and reads `threadAutoSettleEnabled && sidebarAutoSettleOnMerge` while the master setting is off. The Sidebar/ChatView Woke pill composes `hasUnseenWake(...) && !changeRequestAutoSettles(prState, autoSettleOnMerge)` — the parameterized rule replaced the old hardcoded merged/closed check, but upstream's **sticky-woke** semantics were again rejected in favour of open-clears-attention (see [Woke acknowledgement is a superset]).
- **Mobile has no existing-thread Interaction control** (2026-08-14). The user does not use Plan mode, so when upstream's nested bottom-sheet settings (#6224) deleted `thread-settings-menu.ts`, the fork's Interaction (Default/Plan) submenu, `INTERACTION_MODE_CHOICES`, and the `set-interaction` event were dropped rather than transplanted. Do not re-add them. `interactionMode` remains a live contract field on outgoing messages, still set by `ThreadRouteScreen`, the new-task flow, and the CLI's `--interaction-mode` flag — never remove those. Upstream's own new-task inline Build/Plan control (gated by its Legacy Plan Mode preference) is the only mobile Interaction surface.
- **Provider Usage is a native sheet route, and overlays are mutually exclusive** (2026-08-14). `ProviderUsageSheet` was converted from an RN `Modal` to a native form-sheet route alongside upstream's nested settings sheets; its content (per-account quota rows, bars, resets, stale/unavailable states, refresh-on-open) is unchanged. Because settings and usage are now **separate routes that each push themselves**, the navigator does not arbitrate between them: `ThreadComposer` holds a synchronous `overlaySheetOwnerRef` that both open callbacks check and the focus effect releases. It must be a ref, not state — two taps in one frame both read a stale `isActive: false`. Any future composer overlay route needs the same guard. This shipped **without runtime verification** (routine syncs do not run simulators), so it is the first thing to check if mobile sheets misbehave.
- **Mobile send intent is the fork's resolver, never upstream's unconditional send** (2026-08-15). Upstream #6543 ("steer active turns by default") reduced `resolveThreadOutboxDeliveryAction` to `environmentConnected ? "send" : "wait"` and deleted the `activeThreadBusy` prop chain. The fork already reached that default a different way — `use-thread-composer-state.ts` picks `threadIsBusy ? "steer" : "queue"`, and the shared resolver in `packages/client-runtime/src/state/threadOutboxModel.ts` holds only `queue`-intent messages through a running turn. Upstream's version is a strict subset: taking it deletes long-press **Queue for later** silently, because nothing else holds a message through a turn. The fork had independently removed `activeThreadBusy`, so upstream's deletions merged as no-ops — do not read that as agreement about the resolver. Any future upstream edit to the busy gate resolves to the fork's intent-aware branch, and upstream tests written against the old `threadBusy` field get ported to `threadStatus` + `deliveryIntent`.
- **The draft flush gate lives in the update flow, not in the flush** (2026-08-15). Upstream's OTA restart contract wants `flushPendingWrites` to reject so a silent background restart holds off, but the fork's `flushComposerDrafts` is deliberately best-effort: it swallows write failures and requeues, and three fire-and-forget callers (`App.tsx` background transition, `ThreadComposer` blur, `NewTaskDraftScreen` blur) rely on that. The bridge is `hasUnpersistedComposerDrafts()`, which `defaultFlushPendingWrites` in `features/updates/app-updates.ts` checks after flushing. Do not make the flush itself throw — that turns every blur into an unhandled rejection and breaks the existing retry/backoff tests.
- **AUR release publishing is inert here, like the EAS workflow** (2026-08-15). Upstream's `release.yml` now calls a new `publish-aur.yml` after every successful release, and `packaging/aur/scripts/release.sh` hardcodes `pingdotgg/t3code` — so in this fork it would either fail on a missing upstream tag or push upstream assets to the canonical AUR repo. It is inert only because **Release** is `disabled_manually` in `pandec/t3code`; `publish-aur.yml` itself has no push or schedule trigger. Take both files verbatim to stay conflict-free, as with [Never enable the Mobile EAS Production workflow]. Re-check enablement if the fork ever runs its own release.
- **Archive is upstream's on web, the fork's on mobile** (2026-08-16). Upstream #6526 added its own unconditional `archive` entry to the shared builder (after `copy-thread-id`, before `delete`, disabled via the required `isRunning` state), duplicating the fork's. The fork's menu entry and `forkExtras.canArchiveNow` were dropped: `ThreadActionMenuForkExtras` is now exactly `moveToTop` + `fork`. Upstream's entry dispatches through the fork's richer `attemptArchiveThread` (confirmation, in-flight dedupe, undo, wake acknowledgement, cache refresh, forward navigation), so upstream's inline `case "archive"` handler was removed from **both** `Sidebar.tsx` and `useThreadActionMenu.ts` along with their now-unused `archiveThread`/`confirmThreadArchive` reads. Mobile is untouched — upstream ships no mobile archive action, so `thread-list-items.tsx` and `thread-list-v2-items.tsx` keep the fork's menu entry **and** swipe-to-archive. Never re-add an archive entry to `forkExtras`.
- **The fork's running-turn composer supersedes upstream's reachable-send prop** (2026-08-16). Upstream #4781 added `showSendWhileRunning` (mobile viewport only) so a send button appears beside Stop during a turn. The fork's `ComposerPrimaryActions` running branch already offers Stop + **Steer** + **Queue for later** on every viewport, and returns early — leaving upstream's tail unreachable. The prop, its `ChatComposer` plumbing, its stop-button sizing branch, and the three upstream tests written against it were removed. Same shape as [Mobile send intent is the fork's resolver]: upstream's mechanism cannot express Queue for later.
- **Claude config-dir resolution has one owner** (2026-08-16). Upstream #5488 added an inline `resolveClaudeConfigDirPath` to `ClaudeSkills.ts`; it was deleted in favour of the fork's existing `ClaudeHome.ts` export, which the file already imported and which additionally honours a `HOME` override (needed by shadow homes — see [[claude-credential-store-split]]). Upstream's `.agents/skills` discovery root was kept. If upstream edits its copy again, port the behavior into `ClaudeHome.ts`, never restore a second resolver.
- **Both Claude SDK probes must disable hooks** (2026-08-16). Upstream #4466 set `settings: { disableAllHooks: true }` on the capability probe only. The fork runs a _second_ SDK probe — the skills picker's `listSkills` query in `ClaudeAdapter.ts` — which loads `project`/`local` setting sources and so would run workspace `SessionStart` hooks every time the `$` picker opens. It carries the same option, pinned by an assertion in `ClaudeAdapter.test.ts`. Any new probe needs it too.
- **Settle clears an indefinite snooze** (2026-08-16). Upstream #5379 made settle wake a snoozed thread, testing `snoozedUntil != null` only. The fork's "until I wake it" snooze carries a **null** wake time, so settle must test `snoozedUntil != null || thread.snoozedAt != null`, matching the pin-promotion paths already in `decider.ts`. Pinned by a regression test in `decider.settled.test.ts`.
- **The Dev DMG ships as the release DMG** (2026-08-16). Upstream #6201 added channel-specific DMG artwork (`latest`/`nightly`) and a volume title from `resolveDesktopProductName(version)` with its release-only default. The user deliberately wants the packaged Dev install to look like the production build, so upstream's file is taken **verbatim**: a Dev DMG gets the light `latest` artwork and the `T3 Code` volume name while the app inside keeps full Dev identity, icon, and `dev-packaged` state dir. Do not add a `dev` background variant or thread `flavor` into the DMG title — that is a rejected option, not an oversight. Everything else in the flavor chain still needs `options.flavor`; see the packaging watchpoint.
- **Sibling checkouts share the host's rate-limit wrapper** (2026-08-17). In `listWorkspaceProjects`, `withRateLimitBackoff` is applied once per project right after `registry.get(kind)`, so the `checkoutsByRepository` candidates a mutation may fall back to carry the same wrapper as the `supported` entries. Upstream wraps only `supported`; taking that placement lets a mutation routed through a healthy sibling spend provider budget without recording it, and the host never pauses. The wrapper is a pure constructor over one shared host-keyed `Ref`, so per-project construction costs an allocation and nothing else. Re-apply the hoist every time upstream reshapes that loop; `PullRequestService.test.ts` pins it with a mutation-verified test ("spends the shared host budget when a mutation falls back to a sibling checkout").
- **New upstream lint rules land on fork-only files last** (2026-08-17). Upstream migrates its own call sites in the same commit that adds a rule, so a new error-level rule arrives green upstream and red here. #7209's `no-native-title-tooltip` cost six migrations in `Sidebar.tsx` and `SessionImportDialog.tsx`. Whenever `oxlint-plugin-t3code/` gains a rule, grep the fork tree for the pattern and subtract upstream's own migration list before running `vp check` — the rule's own test file states its exact scope (this one skips custom components and five embedded-content elements, so `<Button title>` and `<SidebarMenuButton title>` are fine and `<button title>` is not).
- **Project CLI requires the live server** (2026-08-05). Do not restore writable or read-only local SQLite fallbacks for project commands. Listings and mutations fail clearly when the selected server is unavailable; the strict read-only persistence layer, schema-ledger guard, and fallback tests remain deleted. Revisit only if an offline CLI mode is deliberately redesigned with an explicit consistency contract.

- **Custom-scheme privileges are upstream's, with no fork delta** (2026-08-04). The fork's top-of-`main.ts` `registerSchemesAsPrivileged` (added by the ElevenLabs commit `58ce8ac8f` so `getUserMedia` sees `t3code://app` as a secure context) was deleted in favor of upstream's `ElectronProtocol.registerDesktopSchemePrivilegesSync()` via `DesktopPreReadyPlatform.layer`. `standard`+`secure` still cover mic capture. Upstream omits the fork's old `stream: true`, which was verified inert: recording uses `blob:` URLs and the TTS player resolves its src against `preparedConnection.httpBaseUrl`, so no media element ever loads over the scheme. If voice playback ever regresses, re-add `stream: true` to both entries rather than restoring the `main.ts` block.
- **Pre-ready startup must see the packaged-Dev flavor** (2026-08-04). Upstream's `DesktopEarlyElectronStartup` derives Linux WM class and the early settings path from `VITE_DEV_SERVER_URL` alone, which would send a packaged Dev build to `t3code` and `~/.t3/userdata`. `apps/desktop/src/app/DesktopBuildFlavor.ts` holds the shared `__T3CODE_BUILD_DESKTOP_FLAVOR__` read; `DesktopPreReadyPlatform` passes `isPackagedDevBuild` into the early resolver, and `resolveDesktopStateDir` branches to `dev-packaged` ahead of the `T3CODE_HOME` check. Any new pre-ready code that needs dev identity must use this, never raw `isDevelopment`. Revisit if upstream parameterizes early startup by identity itself.
- **Linux URL handler derives identity from `usesDevelopmentIdentity`** (2026-08-03). `DesktopLinuxUrlHandler.ts` must pass `environment.usesDevelopmentIdentity` (not `isDevelopment`) to `getDesktopScheme` and write an identity-suffixed entry name (`t3code-dev-url-handler.desktop` for dev identity), matching every other fork scheme-derivation site, so a packaged Dev build claims the dev scheme without clobbering the production handler entry. Revisit if upstream parameterizes the handler by identity or entry name itself.

- **Background liveness is presentation-only; provider-process protection is bounded** (2026-08-06, updated 2026-08-09). `backgroundLiveness` (Working/Monitoring) does not block settling, snoozing, archiving, cache persistence, or prewarm. Settling must not immediately stop a session while background work is live; archiving still stops unconditionally. The reaper guard retains upstream's `provider.session.reaper.skipped-background-work` event and fields, bounded by the fork's `providerSessionReaperMaxPendingExtensionMs` lease so leaked volatile liveness cannot pin a provider process indefinitely; expiry is logged as `background_work_expired`, while an overlapping `hasPendingWork` lease retains `pending_work_expired`. Deferred candidate follow-ups: provider-instance provenance in persisted task activities and mobile Working/Monitoring parity.
- **Shell resume across restarts sends a snapshot** (2026-08-06). `ws.ts` captures `shellSequenceAtProcessStart`; a `subscribeShell` resume cursor from before this process falls back to a fresh snapshot because `backgroundLiveness` is volatile and no durable event corrects it. Keep this guard whenever upstream reshapes shell subscription/resume.
- **Liveness clearing is generation-guarded** (2026-08-06). `ProviderRuntimeIngestion` clears thread background liveness on `session.exited` only when the event's `sessionGenerationId` doesn't belong to a replaced generation, mirroring `mirrorSessionCwdOntoThread`. Do not revert to upstream's unconditional clear.
- **Subagent snapshots never advance the parent resume cursor** (2026-08-06). In `ClaudeAdapter.handleAssistantMessage`, a `parent_tool_use_id` assistant snapshot refines the owning task's model and returns; upstream's cursor update was dropped because sidechain UUIDs break cold resume. The pinned fork test guards this.
- **Workflow-script roots derive from configured Claude homes** (2026-08-06). `readWorkflowScript` accepts server-derived `permittedRoots`; `ws.ts` computes them from every `claudeAgent` instance's config dir (shadow homes resolve to the shared root via the `projects` symlink) plus `~/.claude/projects`. Never revert to the hardcoded home-dir root, and never accept client-supplied roots.
- **Claude never receives the shared attachment-store root** (2026-08-09). Upstream's `serverConfig.attachmentsDir` `additionalDirectories` grant was rejected because the store is flat across all threads and also holds TTS audio. Claude keeps cwd-only directory grants; image bytes are still inlined, and injected attachment paths use the normal permission flow. `ProviderService` sanitizes attachment names and rejects foreign-thread attachment IDs before routing. Revisit only if storage becomes per-thread or the SDK gains per-file grants.
- **Background Stop state is thread-keyed** (2026-08-06). `ChatView.tsx` uses `stoppingBackgroundThreadKey` (matching `unsettlingThreadKey`), not upstream's component-wide boolean with reset-on-switch. Preserve on future ChatView merges.

- **Thread pagination is a hybrid; the legacy bridge is now client-only** (2026-08-07, updated 2026-08-07). Upstream's turn-window protocol (`turnLimit`/`beforeCursor` cursor, snapshot `page`, `threadSequence` watermark, `threadSnapshotPagination` capability) is authoritative; the fork's client state machine, event coalescing, warm-refresh/revert/teardown hardening, message artifacts, and mobile auto underfill + top-scroll paging are preserved on top. The **server no longer honors `messageLimit`** and `projectThreadDetailSnapshot` never slices (it only strips any inbound `messageWindow`) — that half of the bridge was retired once the phone ran a turn-window build. What remains is deliberate: the client keeps its legacy path for talking to **old servers** that don't advertise the capability, and because client and server share one contract package, the `messageLimit` fields, the `GET /threads/:id/messages` declaration, its handler, and `getThreadMessagePage` must all stay — the client _encodes_ through those schemas, so deleting them would silently stop it sending `messageLimit` and make an old server return unbounded history. Retire the rest only once every server the phone reaches runs 2026-08-07 or later. Web/desktop stay full-history (`messageWindowLimit: null`, no `turnLimit`; the server window is opt-in per request). The mobile 750-message resident ceiling is separate local state that gates automatic refills only — never write it into `page.hasMore` and never trim inside a returned page while keeping its cursor. Prewarm still fetches unwindowed snapshots pending the PR #123 rebase.
- **Thread-detail event list is one shared constant** (2026-08-07, updated 2026-08-07). `apps/server/src/orchestration/threadDetailEvents.ts` exports `THREAD_DETAIL_EVENT_TYPES` (including the fork-only `thread.history-imported`) and `isThreadDetailEvent`; both the `ws.ts` replay predicate and the `threadSequence` watermark SQL must consume it. Never let upstream's inline event list reappear — an omitted fork event would make imported history look already-current. Upstream now `export`s its inline copy so `server.test.ts` can import it from `./ws.ts`; the fork satisfies that with a one-line re-export from `ws.ts` (cycle-free: `threadDetailEvents.ts` imports only contracts). Re-apply that re-export, never the inline function.
- **Woke acknowledgement is a superset** (2026-08-07, updated 2026-08-13). The fork keeps the passive mark-on-open effect (and a single `activeThreadLastVisitedAt` selector, keyed on `routeThreadKey`) in `ChatView.tsx` _in addition to_ upstream's explicit acknowledgements after send/settle/archive, deliberately rejecting upstream's sticky-woke semantics to preserve open-clears-attention and mobile parity. On future ChatView/useThreadActions merges, re-add the passive effect if upstream deletes it again. Upstream's dismissible open-thread Woke banner and its completion-stamped visit effect were adopted on top; upstream's duplicate `activeThreadLastVisitedAt` declaration must be dropped in favour of the fork's (they collide as block-scoped redeclarations). `markThreadVisited` is monotonic, so the two visit stamps cannot fight. The fork's attention core — wake suppression, sticky snapshots, and unconditional admission — lives in `packages/client-runtime/src/state/threadAttention.ts` and is consumed by both apps. Keep `hasUnseenCompletion`, the fork-added plan-ready extraction, and the upstream-owned status ladders local (`resolveSidebarThreadStatus`, `resolveThreadListV2Status`): upstream's status-pill logic now consumes the web plan-ready copy, so moving these would plant permanent fork edits in upstream-active files. In `Sidebar.tsx` the pill composes the shared wake helper with `!changeRequestAutoSettles(prState, autoSettleOnMerge)` — see [Auto-settle is a master gate], whose parameterized rule **replaced** an earlier hardcoded `prState !== "merged" && prState !== "closed"` check. Do not "restore" the hardcoded terminal-state guards: with auto-settle-on-merge off, a merged PR showing Woke is the intended behavior, and a 2026-08-16 audit reported their absence as a defect on the strength of this line's earlier wording.
- **Timeline live-follow is upstream's, with one fork mapping** (2026-08-07). The fork's thread-keyed latch (`isTimelineDetachedForThread`, `resolveTimelineFollowDecision`, `detachedTimelineThreadKey`, the `isDetached` prop, the `(isAtEnd, isUserNavigation)` callback shape, and MessagesTimeline's own capture handlers) was deleted in favour of upstream's `timelineLiveFollowEnabled` plus its scroll-node wheel/touch/pointer/keyboard opt-out listeners and 40px inset-aware re-arm band (#5566) — deliberate, to stay conflict-free here. One fork behaviour is mapped, not dropped: `maintainVisibleContentPosition.size` is `!liveFollowEnabled` (upstream currently hardcodes `true`) so rows resizing above the viewport cannot move what the user is reading. Never let that revert to `false`. Fork keeps dual minimaps, `shouldPositionTimelineAnchor`, and `getAnchoredTurnMetrics`. Mobile is purely additive: upstream's `endFollowEnabled` latch composes with the fork's `requestOlderMessagesIfNeeded` and `onContentSizeChange` paging.
- **Plan progress clears behind the generation guard** (2026-08-07). Upstream clears `threadPlanProgress` on `session.exited` _before_ the liveness switch, which a stale exit would use to wipe a live replacement session's progress. The fork moved that clear next to `clearThreadLiveness` inside the guarded branch and excluded `session.exited` from the turn-event block. Same rule as [liveness clearing]; keep them adjacent whenever upstream reshapes either.
- **Provider failover config follows the panel split** (2026-08-07). Upstream moved `ProviderSettingsPanel` into its own file; the fork's `removeProviderInstanceAndInboundFailovers` (deletion clears inbound failover references) and `isFailoverTargetCompatible`/`failoverOptions` wiring were relocated into `settings/ProviderSettingsPanel.tsx`, while both helpers stay exported from `SettingsPanels.logic.ts`. `ArchivedThreadsPanel` and `ArchivedThreadModelIcon` remain in `SettingsPanels.tsx`. Upstream deliberately no longer strips favorites/model preferences on delete (per-device semantics) — do not restore that.
- **Plan mode is a default-off beta flag** (2026-08-07). `autoOpenPlanSidebar` is gone from the settings contract (it was entirely upstream-owned; the fork never customised it) and `planModeEnabled` replaces it. The fork's `hasStandaloneCommandContext` guard must stay ANDed with `settings.planModeEnabled` for `/plan` and `/default`, and the composer slash list keeps upstream's `planModeUiEnabled` spread alongside the fork's `isServerThread` `t3-*` commands.
- **The fork's sidebar work lives in `Sidebar.tsx`; leave `LegacySidebar.tsx` as-is** (2026-08-08, reaffirmed 2026-08-11). Upstream promoted v2: `SidebarV2.tsx` was deleted, its content became `Sidebar.tsx`, and the old v1 became `LegacySidebar.tsx`. Git sees no rename, so **never let git 3-way this pair directly** — it produces a full-file conflict and silently drops fork work. Instead transplant explicitly with `git merge-file`: new `Sidebar.tsx` = 3-way(base `<old-main>:SidebarV2.tsx`, ours `dev:SidebarV2.tsx`, theirs `<new-main>:Sidebar.tsx`); `LegacySidebar.tsx` = 3-way(base `<old-main>:Sidebar.tsx`, ours `dev:Sidebar.tsx`, theirs `<new-main>:LegacySidebar.tsx`). That turned a 6.5k-line rewrite into 10 + 3 small conflicts. Upstream resumed editing `LegacySidebar.tsx` in the 2026-08-11 sync, but the user chose no proactive trim or parity work: preserve current fork additions when compatible and revisit only if a concrete future conflict makes the maintenance cost material. Fork-only helpers renamed to upstream's de-suffixed convention (`sortActiveThreadsForSidebar`, `nextSidebarThreadBumpAt`); the persisted `sidebarV2*` **settings keys were deliberately left alone** because renaming them would reset stored preferences.
- **Beta flags are fresh keys with inverted meaning** (2026-08-08). Upstream retired `sidebarV2Enabled`/`sidebarV2ConfiguredByUser` for `legacySidebarEnabled` (default false) and `enableAssistantStreaming` for `enableLegacyTokenStreaming` (default false), deliberately dropping the old keys on decode so every user resets. Fork code must read `!useLegacySidebarEnabled()` where it meant "v2 UI". `BetaSettingsPanel.tsx` and `/settings/beta` are gone; the fork's auto-settle gating on `threadAutoSettleEnabled` now lives in the General panel's auto-settle row in `SettingsPanels.tsx`. This supersedes the 2026-07-28 "Beta flags resolve through a hook" entry's key names, not its rule.
- **Thread action menu is one shared builder with a fork extras block** (2026-08-08, reaffirmed 2026-08-11). Upstream owns the unconditional `copy-thread-id` base action. The fork's optional `forkExtras` field is exactly `moveToTop` + `fork` + `canArchiveNow`; archive remains visible-but-disabled while busy, and `snoozePresets` stays widened to admit the fork's indefinite preset with a null wake time. Both `Sidebar.tsx` and `useThreadActionMenu.ts` pass `forkExtras`, so upstream edits to either menu touch fork code. `moveToTop` remains gated on `!legacySidebarEnabled` because `LegacySidebar.tsx` never reads `movedToTopAt`. Keep one copy handler per surface with the fork's `target: "thread ID"` error label, and never re-add `copy-thread-id` inside `forkExtras`.
- **Fork eligibility is one shared client predicate** (2026-08-09). `canForkConversation` moved out of upstream-owned `apps/web/src/components/Sidebar.logic.ts` into `packages/client-runtime/src/state/threadFork.ts` (`@t3tools/client-runtime/state/thread-fork`); web and mobile both call it, so it is no longer fork code sitting in a file upstream edits. It is a deliberate partial mirror of the server's `requireThreadForkable`: it checks provider, status, active turn, archived, and prior fork failure, but not `providerInstanceId`, so a fork it allows can still be refused. If upstream adds a fork clause server-side, update this predicate too — its docblock names the known gap. `canArchiveThreadNow` stays web-only in `Sidebar.logic.ts`; mobile has no archive gate, so do not "share" it without one.
- **Mobile connection status is upstream's, plus the prewarm spinner** (2026-08-08). Upstream's `WorkspaceConnectionTitle.tsx` superseded the fork's `HomeHeaderConnectionStatus.tsx` (deleted): it adds an 800 ms debounce, a JS-driven fade that survives native header items, and covers `ThreadNavigationSidebar` as well as `HomeScreen`. The fork's background-prewarm indicator was grafted on as `showThreadSync`, which owns the `useThreadPrewarmSummary` + `useMinimumVisibleFlag` read internally so it stays live inside memoized header options. `workspace-connection-status.ts` now exports only upstream's `{label, showsProgress} | null` shape — the fork's `shortLabel`/`synchronizing`/`isWorkspaceConnectionSynchronizing` are gone, and with them the fork's masking of raw connection errors behind "Connection issue".
- **Bulk snooze shows one aggregate toast** (2026-08-07). Upstream's count-based aggregate toast + aggregate Undo replaced per-thread toasts, keeping the fork's indefinite presets, null `snoozedUntil` payloads, and batch navigation exclusion. Single-thread toasts build titles via `snoozedUntilToastTitle` in `Sidebar.snooze.ts` — the one place enforcing that `snoozeWakeDescription` is never called with null (indefinite renders "Snoozed until you wake it").

- **Never enable the Mobile EAS Production workflow as upstream ships it** (2026-08-10). Upstream added a `push: branches: [main]` auto-release trigger that builds, auto-submits to TestFlight/Play and publishes production OTAs. In this fork `main` is the upstream mirror, and the workflow targets upstream's EAS project and App Store app ID `6787819824`, not the fork's `6795398212`. It is currently inert only because the workflow is `disabled_manually` in `pandec/t3code` — the file itself is taken verbatim to stay conflict-free. The user deliberately keeps mobile releases manual (they move between wired builds, TestFlight and EAS by hand). Enabling it would require retargeting the trigger to `dev`, injecting `T3CODE_EAS_PROJECT_ID`/`T3CODE_EAS_OWNER` before the first `eas env:pull`, and switching the submit ID. `mobile-fingerprint-check.yml` is separate, advisory-only, needs no secrets, fires on PRs to any base, and is kept as-is.
- **Codex usage skips are counted, not guessed** (2026-08-10). `parseCodexLine` returns `null` both for lines it cannot parse and for well-formed `token_count` lines it deliberately drops. The fork's `malformedRecords` counter means "this file's totals are understated", so `CodexScanState.deliberateSkips` distinguishes the two and `usageTranscriptReader` charges only genuine parse failures. Upstream's 2026-08-10 fork-copy suppression would otherwise have flagged every forked or subagent rollout as incomplete. Increment `deliberateSkips` at any new intentional `return null` — but **never on a path a corrupt line can reach**: the zero-total check deliberately does not increment, because `int()` coerces a non-numeric field to 0 and a corrupted payload is indistinguishable from an empty event there. `usageTranscriptReader.test.ts` covers the seam end-to-end.
- **Fork long-press call sites must accept `delayLongPress`** (2026-08-10). Upstream's iOS tap-through guard in `ControlPillMenu` clones the child with `delayLongPress: 350`, which only works if the child forwards it to a `Pressable`. The fork's `ControlPillMenu shouldOpenOnLongPress` around the composer send button (queue-for-later) wraps `ComposerToolbarButton`, which upstream never wraps, so the prop had to be added and forwarded there. When adding a fork long-press menu around any component, check it forwards `delayLongPress` or the hold-and-release guard is silently inert and the press taps through.
- **Usage windows are upstream-owned; coverage remains fork-owned** (2026-08-11). Use upstream's daily/hourly window model and rolling Past 24h query identity, including `resolution`, `sinceTime`, and `untilTime`. Preserve the fork's partial-source filtering, progress/error/unreachable classification, and `partialSources` notice. `usageCoverage.ts` stays in `apps/web`; any shared refresh helper must preserve the current resolution and compare both time bounds.
- **Mobile interaction structure is upstream's, with fork durability around it** (2026-08-11, updated 2026-08-12). Keep upstream's nested settings navigation, Android `KeyboardStickyView`, iOS native settings menu, pending-input ownership, platform bottom inset, extracted live-follow state machine, and capability-gated thread-title regeneration. Preserve fork durable composer drafts, interruption recovery, Queue for later, interaction mode, voice/provider-usage/CWD skills, pagination, underfill/retry, measured insets, anchor consumption, queued sends, diagnostics, and the full v2 lifecycle menus. `ThreadSettingsSheet` and the iOS menu share choices through `thread-settings-menu.ts`; the menu must retain the fork Interaction submenu/event. In v1 and every v2 row variant, insert **Regenerate title** before destructive Delete without removing Archive, settle/unsettle, snooze/wake, or pin/reorder actions. Mobile live-follow must continue composing with automatic paging rather than restoring the manual Load Earlier row.
- **Sidebar width reset must clear the rail's unclamped preference** (2026-08-12). Upstream's double-click reset (`AppSidebarLayout.resetSidebarWidth`) clears only localStorage and its own state, which is complete against upstream's sidebar but not against the fork's `SidebarRail`: the fork keeps an unclamped `preferredWidthRef` plus a dep-less reconciliation layout effect that re-applies it before paint, so the reset silently snapped back and only took effect after a reload. `SidebarRail` now intercepts `onDoubleClick` and nulls `preferredWidthRef` (leaving `hydratedStorageKeyRef` alone). Any future caller-side width reset must go through the rail for the same reason. Revisit if upstream adopts an equivalent preference cache.
- **Native menu patch is a deliberate union** (2026-08-11). `@react-native-menu/menu` keeps the fork's no-duplicate initialization interaction, `super` tap path, superview-hosted long-press interaction, and hidden-wrapper pointer behavior while carrying upstream nested submenus, `keepsMenuPresented`, stable identifiers/subtitles, and deferred visible-menu refresh. In `configurationForMenuAtLocation`, pending-menu cleanup and presentation bookkeeping occur before the long-press guard. Regenerate the pnpm patch hash after every manual union; preserve the fork RNGH and image-picker mappings.

## Watchpoints

When the incoming upstream range touches a path below, spawn one targeted sub-agent during the behavioral-overlap review to answer that entry's question (is the fork change still needed / still compatible?). Untouched paths need no check.

Housekeeping: the 2026-08-17 sync (`277322933` → `949feb61e`, **9 commits**, 189 files) was mostly mobile built-in themes (#6619 — `useColorScheme` and colour literals replaced by a `themeAppearance` preference plus theme tokens across ~40 mobile files), a Settings → Integrations page with browser defaults (#7082), provider API budget protection for pull requests (#6466, new `SourceControlRateLimit`/`githubGraphQlBudget` plus a `pullRequestsThreadComments` read RPC), and an error-level lint rule banning native `title` tooltips (#7209). Eight targeted reviews ran; **no superseding overlaps** — the first sync in a while needing no user decision. 11 conflicts / 15 hunks; ten were mechanical unions and only `apps/mobile/src/App.tsx` needed a hand transplant, because upstream extracted an `AppContent` component so the theme hooks run inside `AppearancePreferencesProvider` and the fork's four coordinators had to be carried across (upstream's version also fixes a latent fork bug: `useThemeColor` was previously called outside that provider). Three integration defects were fixed that a clean textual merge would not have caught: (1) upstream's new lint rule is error-level and would have failed `vp check` on **six fork-only** native `title` attributes upstream never saw — 4 in `Sidebar.tsx`, 2 in `SessionImportDialog.tsx` — all migrated to the styled `Tooltip`; (2) the fork's sibling-checkout fallback in `listWorkspaceProjects` kept the **raw** provider while upstream wrapped only the `supported` entry, so a mutation routed through a healthy sibling would spend provider budget without recording it — `withRateLimitBackoff` is now hoisted so both paths share the host-keyed backoff, pinned by a mutation-verified test in `PullRequestService.test.ts`; (3) `ProviderUsageSheet`'s Android header had the same status-bar exposure #6635 fixed in Thread Settings, so it now takes `Math.max(insets.top, 12)` on Android only. One fork-only mobile test broke on a transitive import: `thread-list-v2-items.test.ts` is unchanged by the merge, but its subject now imports `AppearancePreferencesProvider`, which pulls in `uniwind` and fails to transform — fixed with a `vi.mock` beside the existing ones. My own first union of `contracts/settings.ts` left a `Schema.pipe(` unclosed; the formatter caught it, not typecheck. Three post-merge audits (web/mobile/server) found **no** merge-introduced defects. No migration landed, so numbering stays at **048**. Two corrections to earlier ledger wording: `apps/server` **is** now in the `vp run test` task graph (task 14 of 14), contradicting the older note that it is skipped — but it and `infra/relay` both still exited 137 under parallel load and passed alone, so the OOM half of that note stands. Deliberately left unfixed and reported instead: four fork-only mobile surfaces still use hardcoded neutral/zinc colour literals and so ignore the new built-in themes (`ComposerCommandPopover`'s Manual label, `QueuedMessageList`, and two `ThreadFeed` regions) — all pre-existing and cosmetic; and upstream's own new `threadComments` single-flight key in `packages/client-runtime/src/state/pullRequests.ts` omits the project/repository/PR from its key, so two concurrent loads sharing a thread ID and cursor across hosts could coalesce — upstream-owned, no fork delta.

Housekeeping: the 2026-08-16 sync (`8c628f149` → `277322933`, **93 commits**, 268 files — an unusually large drop) was mostly a long tail of upstream bug fixes, plus theme-palette restoration and simplified advanced theme controls, day-aware/OS-locale timestamps, workspace-wide file drop, PR line requests to the agent, a macOS DMG installer background, Chrome-style hold-to-quit, bounded thread-activity hydration, a persisted wire projection for streaming tool updates, and repo-local `.agents/skills` discovery. Ten targeted reviews ran; every watchpoint was complementary. Two supersessions needed user decisions — the duplicated Archive menu entry and the Dev DMG artwork — both now standing decisions above; a third (mobile reachable-send) was the fork already doing more, as in the previous two syncs. 31 conflicts / 51 hunks, mostly unions; `ChatComposer.tsx` (8) and `Sidebar.tsx` (5) carried the semantic weight. Six defects surfaced at typecheck, four of them real merge damage: upstream's provider-badge peer lookup bypassed the fork's **environment-scoped** provider registry, the predicted `PROVIDER_LABEL` deletion hit the fork's coverage notice, an upstream test omitted the fork's required `autoSettleEnabled`, and upstream's inline Claude config-dir resolver collided with the fork's better one. Three post-merge audits then found two more that compiled cleanly — settle not clearing an indefinite snooze, and the skills-picker probe running workspace hooks — both fixed with mutation-verified regression tests. The web audit also reported three defects that were **false positives**, all pre-existing and byte-identical across the merge: the Woke pill's terminal-state guards (see the corrected wording in [Woke acknowledgement is a superset]), `CommandPalette.tsx` using raw `resolveThreadPr`, and the PR-snapshot writer living only in `Sidebar.tsx` — the latter two are upstream's own gaps (upstream's `LegacySidebar.tsx` and `ThreadStatusIndicators.tsx` resolve raw too) and were left alone. No migration landed, so numbering stays at **048**. `icons:check` is not runnable on this host (needs Icon Composer 2.x `ictool`; it fails at tool resolution before reading any asset), even though upstream replaced the three desktop icon binaries. `src/server.test.ts > routes websocket rpc projects.listEntries and projects.readFile` failed once in four full server runs with a connect-level `fetch failed` on an ephemeral port and passed 8/8 in isolation — parallel-load flake, not a regression. Untouched by upstream this sync (first strike toward removal): the liveness/plan-progress/workflow-script watchpoint, `usageTranscripts`, the mobile row-menu watchpoint, the mobile composer-overlay watchpoint, the outbox-delivery-gate watchpoint, and the draft-flush watchpoint. The 2026-08-15 sync (`7e01d33f0` → `8c628f149`, 22 commits) landed a large web global-styling simplification that moved component chrome into the shared `ui/` primitives, day-aware chat timestamps, opening remote environments in a local editor over SSH, a Windows `server.asar` sidecar packaging rewrite with payload validation, in-repo AUR packaging, terminal subprocess polling that takes one process-table snapshot per tick, and a mobile OTA-restart-safety pass (atomic draft/outbox writes, flush-before-reload, foreground-handoff around the image picker). Ten targeted reviews ran; nine were complementary. The tenth (mobile steer-by-default) was reported as superseding but resolved as complementary on inspection — the fork already defaulted to steering and upstream's mechanism could not express Queue for later, now recorded as a standing decision above. 21 conflicts / 31 hunks, nearly all small unions; five mobile files took the fork side wholesale because upstream's change there was a no-op against work the fork had already done. Three integration defects were found and fixed post-merge, none of which a clean textual merge would have caught: upstream's new Windows payload validator called `resolveDesktopProductName(appVersion)` release-only and would reject a Dev executable; an upstream test auto-merged in using the deleted `threadBusy` field (failed typecheck, ported to the fork's shape); and upstream's new OTA restart gate silently accepted the fork's swallowing flush. Two defects in upstream-owned code with no fork delta were reported and deliberately left unpatched: the AUR publish job's hardcoded upstream repo (inert, see the standing decision) and a WSL sidecar extraction cache keyed only on `appVersion`, so a rebuilt Windows Dev artifact reuses stale extracted server code — Windows is not a fork surface. Upstream also orphaned its own `--app-theme-terminal-scrollbar[-hover]` variables in the CSS simplification, so custom-theme terminal scrollbar colors no longer reach Ghostty; also upstream-owned, also left. No migration landed, so numbering stays at 048. The 2026-08-14 sync (`6bc6cb6be` → `7e01d33f0`, 23 commits) landed the Browser-panel favicons and browser-ready local-server discovery, nested mobile task settings in bottom sheets, a per-reason auto-settle-on-merge toggle, a Windows asar packaging rewrite, and assorted web/mobile fixes. Six targeted reviews ran; the only superseding overlap was the mobile settings restructure, decided by the user as: **drop** the fork's existing-thread Interaction control, and **convert** Provider Usage to a native route (both now standing decisions above). 30 conflicts — mostly small unions, with five mobile files hand-transplanted onto upstream's versions rather than 3-way merged. Four integration defects were found and fixed post-merge, none of which a clean textual merge would have caught: `#6414`'s minimap fix applied to only one of the fork's two minimaps; `CommandPalette.tsx` omitting `autoSettleOnMerge`; the two new overlay routes able to stack sheets; and three upstream `vi.mock` tests missing from the web isolation allowlist. One rename fallout needed a manual fix — `VoiceRecorderControl.tsx` still imported `ComposerToolbarTrigger` after upstream renamed it to `ComposerToolbar` (git tracked the file rename, not the import string in fork-only code). No migration landed, so numbering stays at 048. Two pre-existing (not merge-introduced) build issues were reported and deliberately left: `--flavor dev --skip-build` can package release runtime identity under Dev metadata, and Linux Dev artifacts advertise the production `t3code` URL scheme alongside `t3code-dev`. The 2026-08-13 sync (`b73232bdd` → `6bc6cb6be`, 14 commits) landed the multi-host pull-request workspace overhaul (filters and qualifiers, all-server listing, update branch, reactions, in-place editing, smarter diffs), plus command-subtitle project icons, PR links opening in the browser, draft-repo prompt carry, and assorted web fixes. Three targeted reviews ran (PR identity, sidebar, ChatView/ws.ts) and every overlap was complementary. Five conflicts, all small: three import-block unions, one deferred-filter placement, one draft-seed union. Two integration fixes were applied — `verifyLive` on the three new PR mutations, and `inputOrigin` carry in `moveComposerPromptAndImages` — both recorded as standing decisions above. No migration landed, so numbering stays at 048. Open P2 (pre-existing, not merge-introduced): `refineUnknownProjectKinds` applies no project narrowing at all, so a single-project or `projectIds` listing can still probe every unrelated unknown host; narrowing it needs a sibling-scoping rule and was deliberately left out of a routine sync. The second 2026-08-12 sync (`5a8461480` → `b73232bdd`, 7 commits) added the right-panel surface launcher with letter shortcuts, sidebar double-click width reset, Clerk surface theming, CLI OAuth loopback sign-in through hosted `/connect`, mobile ordered-list bubble sizing, and changed-files header container queries. It merged with zero conflicts (the fork had no delta in any upstream-touched file except `ChatView.tsx`/`ThreadFeed.tsx`, which auto-merged). One genuine integration defect was found and fixed — see [Sidebar width reset must clear the rail's unclamped preference]. The live-follow and sidebar-width watchpoints both ran and were reaffirmed. Several defects in upstream's own new code (launcher letter shortcuts consuming the first character typed into an empty composer, launcher focus-on-mount stealing composer focus, `parseLoopbackPort` accepting any 1–65535 port from the URL fragment, CLI/hosted-bundle deploy-order coupling) were reported to the user and deliberately left unpatched: the files carry no fork delta and patching upstream-owned code raises future merge cost. No migration landed, so numbering stays at 048. The earlier 2026-08-12 sync (`35172010b` → `5a8461480`, 9 commits) added OKLCH theme palettes and Open VSX theme search, compact sidebar footer actions, mobile thread-title regeneration, composer/sidebar polish, and clearer Git action icons. Mobile title regeneration was merged into every fork lifecycle menu without dropping Archive, snooze, settle, or pin actions; the fork's Back-on-both-footer-pages delta was retired because upstream adopted it, and the fork accepted upstream hiding Settings while Usage or Pull Requests is open. No migration landed, so numbering stays at 048. The 2026-08-11 sync (`9c7622dac` → `35172010b`, 14 commits) added hourly Past 24h usage, theme-aware environment artwork, mobile composer/live-follow stabilization, shared Copy Thread ID, Shift+click new-thread creation, self-hosted GitLab routing, and web navigation/error-banner fixes. Repository-identity mutation verification was retained around upstream provider refinement; usage coverage, mobile durability/paging, packaged-Dev artwork gating, and the native-menu patch union were reaffirmed. No migration landed, so numbering stays at 048. The earlier 2026-08-11 sync (`3d74474f6` → `9c7622dac`, 21 commits) added the multi-provider pull-request workspace and in-app reviews, Android tablet orientation handling, mobile wide-Markdown layout fixes, and desktop confirmation-dialog cleanup. Repository identity was re-reviewed and retained as a live-first persisted fallback; pull-request mutations must verify current live identity. No migration landed, so numbering stays at 048.

Conflict gotchas added 2026-08-10:

- `apps/web/src/usage/` was renamed into `packages/shared/src/` for `usageFormat`/`usageMerge` only. Fork files added to that directory raise "file location" conflicts and git suggests moving them all — but `usageCoverage.ts` imports `@t3tools/client-runtime/connection` and `@t3tools/shared` does not depend on client-runtime, so it must stay in `apps/web/src/usage/`. Only `usageFormat.test.ts` follows its implementation across.
- A test moving into `packages/shared` inherits stricter rules than `apps/web`: relative imports need explicit `.ts` extensions (`node16` resolution) and `new Date()` needs a top-of-file `// @effect-diagnostics globalDate:off`.
- When both sides bump the same cache/schema version to the same number for different reasons (`USAGE_SCAN_CACHE_VERSION`, both to 2), the merged value must be a third number neither side shipped, or entries written by either build survive and are misread.

Verification gotchas — apply on every sync:

- **Every new upstream web test using `vi.mock`/`vi.useFakeTimers`/`vi.resetModules` must be added to `ISOLATED_TEST_FILES` in `apps/web/vite.config.ts`.** The fork runs the web suite with `isolate: false` for speed and keeps an explicit isolation allowlist; an upstream test that mocks a module but is not listed sees its mock silently not applied (and can break _other_ files order-dependently). In the 2026-08-14 sync this surfaced as `PreviewFaviconIcon.test.tsx` failing on the real hook returning null, with `browserFaviconStore.test.ts` and `PreviewEmptyState.test.tsx` latent behind it. Check every added upstream web test each sync — the failure does not name the cause.
- `vp run test` schedules packages in parallel and can OOM on this machine: `apps/mobile`, `infra/relay`, and `apps/server` have all exited 137 (SIGKILL) with no assertion failure. Re-run any 137 package alone before treating it as a regression — on 2026-08-17 all three passed solo (server 3301, relay 207, mobile 974). `apps/server` **is** in the task graph (14 of 14); the older claim that it is skipped entirely was wrong.
- The Bash tool's working directory persists between calls. A `cd apps/web` earlier in a sync silently rebases later `--root` flags and `git ... -- <pathspec>` arguments, which then return empty and look like "nothing changed". Re-anchor with an absolute `cd` before repo-wide commands.

- **This agent session also exports `ELECTRON_RUN_AS_NODE=1`** (2026-08-15), which leaks into spawned build probes and fails `build-desktop-artifact.test.ts > skips the primary native probe for cross-architecture Windows payloads` with a real-looking `expected true to be false`. Clear it the same way: `env -u CLAUDE_CONFIG_DIR -u ELECTRON_RUN_AS_NODE vp run test`. See [[electron-run-as-node-leak]] for the wider damage this variable does.
- Run the suite as `env -u CLAUDE_CONFIG_DIR vp run test`. This agent session exports `CLAUDE_CONFIG_DIR=~/.claude-t3/proxy`, which makes `ClaudeHome.test.ts` and `ClaudeSessionFork.test.ts` fail with a real-looking assertion (`.claude-t3/proxy` vs `.claude`). Those failures are contamination, not regressions.
- Check conflict markers with `awk '/^(<<<<<<<|=======|>>>>>>>)/{print FILENAME":"FNR}'`, not `grep` (the shell's `grep` wrapper silently reports no match for marker patterns) — and use `FNR`, not `NR`, or multi-file output reports meaningless cumulative line numbers.
- **Never `git stash` during the merge.** It silently deletes `MERGE_HEAD`, so the next commit records one parent instead of two. Cost time on 2026-08-08. If it happens, the fix is `echo <main-sha> > .git/MERGE_HEAD; echo no-ff > .git/MERGE_MODE`, then re-stage — worktree contents survive.
- Python `str.replace()` replaces **all** occurrences. When resolving several conflicts in one file by scripted replace, anchor on unique surrounding text or the first `<<<<<<< HEAD` strip will silently open the later ones.
- Check `git status` for a modified root `package.json` before staging: a previous `t3-build`/`t3-install-desktop` run can leave the packaged-Dev artifact manifest there. Staging it wipes the root scripts and devDependencies, which then breaks `vp install` and makes `typecheck` report "task not found". Restore with `git checkout <dev-sha> -- package.json`.
- Taking upstream's `pnpm-lock.yaml` wholesale fails `--frozen-lockfile`: the fork carries extra `patchedDependencies` (e.g. `expo-image-picker`) and its own RNGH patch hash. Run `vp install --no-frozen-lockfile` to regenerate, then re-verify with `--frozen-lockfile`.

- `apps/web/src/components/settings/SettingsPanels.tsx`, `settings/ProviderSettingsPanel.tsx`, `settings/SettingsPanels.logic.ts`, `settingsSearch.ts`, or `settingsLayout.tsx`: if upstream splits or restructures the settings panels again, did the fork's provider-failover wiring, `ArchivedThreadsPanel`/`ArchivedThreadModelIcon`, `/settings/extras` route and its seven search entries, and the `useSettingsRestore` dirty/reset coverage all survive the move? Added after the 2026-08-07 theme-library + per-device-provider split, where taking either side wholesale would have silently deleted fork behaviour.

- `apps/web/src/components/Sidebar.tsx` or `apps/web/src/components/settings/ProjectSettingsPanel.tsx`: if upstream changes project settings or project-group actions, do all four ported fork capabilities still have a home on the contextual page (accent picker, logical archived-thread navigation, checkout session import, scope/hidden-filter cleanup), and has dead inline-dialog code stayed deleted? Checked 2026-08-10 — upstream rewrote the panel around contextual routes and per-checkout selection; all four were transplanted by hand rather than 3-way merged, because the file is effectively a rewrite. Expect to transplant again, not merge.

- `apps/server/src/pullRequest/PullRequestService.ts` or `apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.ts`: do read-only PR surfaces still tolerate persisted repository identity while every external mutation verifies a current live checkout identity or selects a healthy sibling checkout? Never let stored fallback alone authorize a provider action. Added 2026-08-11 when upstream's live-only PR service met the fork's durable identity fallback; extended 2026-08-13 — also check that every mutation upstream added carries `verifyLive` and that new `listWorkspaceProjects` filters stay deferred past `checkoutsByRepository` (see [Every new PR mutation needs `verifyLive`]).

- `apps/web/src/components/usage/UsagePage.tsx`, `apps/web/src/state/usage.ts`, or `packages/shared/src/usage*.ts`: if upstream reshapes the usage page chrome again, do the fork's `selectWindow`/`refreshWindow` midnight handling and `partialSources` coverage notice survive, and is `usageCoverage.ts` still in `apps/web` rather than dragged into `shared`? Added 2026-08-10 after upstream re-indented the whole page body and relocated the format helpers.

- `apps/web/src/components/chat/MessagesTimeline.tsx`, `chat/timelineScrollAnchoring.ts`, the follow/anchor block in `apps/web/src/components/ChatView.tsx`, or `apps/mobile/src/features/threads/ThreadFeed.tsx`: if upstream reshapes live-follow again, is `maintainVisibleContentPosition.size` still `!liveFollowEnabled` (not upstream's hardcoded `false`), do the fork's dual minimaps and anchor helpers still compile against upstream's callback shape, and does mobile still run fork paging alongside `endFollowEnabled`? Added after the 2026-08-07 decision to adopt upstream's latch.

- `apps/web/src/components/Sidebar.tsx`, `apps/web/src/components/LegacySidebar.tsx`, `apps/mobile/src/features/threads/threadListV2.ts`, or thread pin/move contracts and projectors: if upstream changes pin classification or ordering, does `pinOrderKey` still order only the pinned partition while `movedToTopAt` affects only unpinned active rows, does pinning still wake both timed and indefinite snoozes, and is pinned sorting still capability-independent? Added after the 2026-08-05 pin merge; reconfirmed complementary on 2026-08-06 and again on 2026-08-08 when upstream added drag-to-reorder.

- `apps/web/src/components/threadActionMenu.logic.ts`, `apps/web/src/hooks/useThreadActionMenu.ts`, `apps/web/src/components/chat/ChatHeader.tsx`, or `apps/web/src/components/Sidebar.snooze.ts`: if upstream extends the shared thread action menu, does the fork's `forkExtras` block still slot in at the right ordering position, does the widened `snoozePresets` type still admit the indefinite preset, and has upstream grown its own equivalent of any fork entry (then drop the fork's)? Added 2026-08-08 when the fork's inline menu was folded into upstream's builder; extended to `ChatHeader.tsx` on 2026-08-10, when upstream rewrote the header into `WorkspaceBreadcrumb` and the menu wiring survived only because the fork had no delta in that file.

- `apps/mobile/src/features/threads/thread-list-items.tsx`, `thread-list-v2-items.tsx`, or `thread-title-regeneration-menu.ts`: if upstream changes mobile row menus again, does capability-gated title regeneration remain immediately before destructive Delete in every v1/v2 variant while the fork's Archive, settle/unsettle, snooze/wake, and pin/reorder actions remain intact? Added 2026-08-12 when upstream's smaller title-regeneration menu arrays would have removed fork lifecycle actions if taken wholesale.

- `scripts/build-desktop-artifact.ts` or `apps/desktop/vite.config.ts`: if upstream reworks desktop packaging again, does the fork's `--flavor` flag still reach the desktop build as `T3CODE_DESKTOP_BUILD_FLAVOR` (feeding `__T3CODE_BUILD_DESKTOP_FLAVOR__` → `DesktopBuildFlavor.ts`), and does upstream's external-package unpack allowlist still cover what the Dev artifact needs? Added 2026-08-14, when upstream's asar rewrite (#5877) merged into this file with **zero conflicts** despite both sides editing imports, staging, and `buildDesktopArtifact` — a clean merge here proves nothing, because losing the env propagation silently produces a Dev-branded app running release identity and state paths. Known pre-existing hole found the same day: `--flavor dev --skip-build` reuses `dist-electron` without checking its compiled flavor, so it can package release runtime identity under Dev metadata. Extended 2026-08-15: every new upstream helper that derives a packaged name needs `options.flavor` threaded through — upstream's new Windows payload validator called `resolveDesktopProductName(appVersion)` with its release-only default and would have rejected a Dev executable. Check each such call site, because the parameter is optional and a missing argument compiles.

- `apps/mobile/src/features/threads/ThreadComposer.tsx`, `ProviderUsageSheet.tsx`, or `use-thread-settings-sheet-presentation.ts`: if upstream adds another composer overlay or changes sheet presentation, does every overlay still go through the synchronous `overlaySheetOwnerRef` guard, and does Provider Usage still present as a native route? Added 2026-08-14 with the modal→route conversion; two route-pushing overlays with independent state machines will stack sheets without it.

- `apps/mobile/src/lib/composerImages.ts`: if upstream moves or adds an attachment-type guard, does `isProviderSendTurnSupportedImageMimeType` still run on the **post-re-encode** mime type in both the picker and paste paths? Added 2026-08-16: the auto-merge placed upstream's guard above the fork's JPEG conversion, referencing `mimeType` before its declaration — typecheck caught that instance, but a guard placed after the declaration and before conversion would compile and silently reject every HEIC pick. The incoming-share path deliberately has no re-encoder and rejects outright.

- `apps/server/src/provider/Layers/ClaudeAdapter.ts` `listSkills` query options, or `ClaudeProvider.ts`'s `buildClaudeCapabilitiesProbeQueryOptions`: when upstream hardens one Claude SDK probe (hooks, MCP, setting sources, tool allowlists), does the fork's second probe get the same treatment? Added 2026-08-16 — the two probes are structurally independent and upstream only ever sees its own.

- `apps/web/src/components/chat/ContextWindowMeter.tsx` and its `ChatComposer.tsx` props: upstream owns the context-window readout and now names the selected model in the compaction note (`modelDisplayName` + `formatContextWindowCompactionMessage`), while the fork wraps the same component with the whole provider-usage account panel. If upstream reshapes the meter's props again, does the fork's usage panel still mount, and did an upstream prop rename leave a fork prop dangling? Added 2026-08-16, when upstream's rewrite of the props block would have deleted the panel wholesale.

- `apps/web/src/composerDraftStore.ts`: if upstream adds or reshapes a helper that relocates composer content between drafts, does `inputOrigin` travel with the prompt and leave the drained source, so voice provenance survives and `shouldRemoveDraft` can still prune? Added 2026-08-13 when upstream's draft-repo prompt carry landed without it.

- `apps/web/src/components/threadSidebarWidth.test.ts`, `apps/web/src/components/AppSidebarLayout.tsx`, `apps/web/src/components/ui/sidebar.tsx`, or the sidebar-brand/container blocks in `apps/web/src/index.css`: if either side changes the legal sidebar minimum, rearranges those CSS blocks, or adds another width-reset/restore path, does the wordmark remain visible across the full legal range, does the CSS-contract test still inspect the intended media block, and does every reset still clear the rail's `preferredWidthRef`? Added 2026-08-12 after upstream fixed the fork-visible 208px wordmark gap; extended the same day when upstream's double-click reset was silently undone by the fork's preference cache.

- `apps/mobile/src/App.tsx`: if upstream restructures the root tree again, do all five fork coordinators (`SplashScreenCoordinator`, `ThreadPrewarmCoordinator`, `ComposerDraftPersistenceCoordinator`, `MobileDiagnosticsCoordinator`, `KeyboardStickyResetCoordinator`) still mount, each inside the provider its hooks need? Added 2026-08-17, when upstream's `AppContent` extraction made the file a hand transplant rather than a merge — and note upstream's structure is the correct one: the theme hooks must run _inside_ `AppearancePreferencesProvider`, which the fork's old root did not do.

- Any `apps/mobile/**/*.test.ts` that mocks `react-native` wholesale: when upstream gives a tested component a new provider or theme import, the real module loads and the suite dies at import with a bare `SyntaxError` naming no cause (`uniwind` is the usual culprit, via `AppearancePreferencesProvider`). Add the matching `vi.mock` beside the existing ones. Added 2026-08-17 after `thread-list-v2-items.test.ts` — a file the merge did not touch — failed this way. This is the mobile twin of the web `ISOLATED_TEST_FILES` gotcha below.

## Full audit

- Next cross-feature fork-vs-upstream audit due: **2026-09-08**, or sooner if a sync merges an unusually large upstream drop (roughly >100 commits or a comparably broad changed-code surface).
- Last run: 2026-08-11 — four-domain discovery plus three adversarial verification passes. Confirmed a Linux installer defect, mixed-version capability gaps, stale operational/user documentation, native configuration hazards, and low-severity dead/test-only surfaces.
- Disposition: leave the confirmed findings unchanged for now. The user manually controls server/iOS rebuilds and installs, does not require mixed-version or automated-release compatibility, and considers the remaining cleanup low value. Revisit only if those operating assumptions change or a finding causes a concrete failure.
